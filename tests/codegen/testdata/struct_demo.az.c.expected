typedef struct Point Point;

struct Point {
	int x;
	int y;
};

int distance(Point a, Point b) {
	return a.x + b.y;
}

int main(void) {
	Point a;
	a.x = 3;
	a.y = 4;
	Point b;
	b.x = 4;
	b.y = 3;
	return distance(a, b);
}